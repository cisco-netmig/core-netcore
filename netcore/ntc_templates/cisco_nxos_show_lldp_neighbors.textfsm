Value NEIGHBOR (\S+)
Value LOCAL_INTERFACE (\S+)
Value REMOTE_INTERFACE (\S+)
Value CAPABILITIES (\w+)

Start
  ^Device.*ID -> LLDP

LLDP
  ^${NEIGHBOR}\s+${LOCAL_INTERFACE}\s+\d+\s+(${CAPABILITIES}?\s+)?${REMOTE_INTERFACE} -> Record
  ^${NEIGHBOR}\s*$$
  ^\s+${LOCAL_INTERFACE}\s+\d+\s+(${CAPABILITIES}?\s+)?${REMOTE_INTERFACE} -> Record
